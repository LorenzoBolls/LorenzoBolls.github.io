import React from "react";
import FoldMap from "../components/FoldMap"; // Adjust the path based on your folder structure

const Experiences = () => {
    return (
        <div id="experiences">
            <FoldMap />
        </div>
    );
};

export default Experiences;
